# ── Node / Express ───────────────────────
PORT=5000

# ── Python Flask microservice URL ────────
# Change host/port if you deploy Flask elsewhere
FLASK_URL=http://localhost:8000

# ── PostgreSQL (fill in when ready) ──────
DB_USER=postgres
DB_HOST=localhost
DB_NAME=shopxp
DB_PASSWORD=yourpassword
DB_PORT=5432

# ── JWT ──────────────────────────────────
JWT_SECRET=your_jwt_secret_here

# ── Flask microservice ───────────────────
FLASK_PORT=8000
# Path to cleaned_transactions.csv (default: same folder as recommend.py)
CLEANED_CSV=cleaned_transactions.csv
